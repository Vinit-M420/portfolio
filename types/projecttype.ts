export type ProjectType = {
    title: string,
    description: string,
    bannerImg: string,
    bannerClass: string | null,
    projectUrl: string | null,
    projectGithub: string | null,
    linkColSpanEach: number,
}